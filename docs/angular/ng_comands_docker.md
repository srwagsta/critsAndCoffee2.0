Executing ng Commands With Docker
=================================

Code scaffolding
----------------
```bash
docker-compose -f test.yml run --rm angular ng generate component <component-name>
```
You can also use `ng generate directive|pipe|service|class|guard|interface|enum|module`.
